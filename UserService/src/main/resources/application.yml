server:
#  port: 9091
  port: 0  # 실행할때마다 포트번호 random 배정

spring:
  application:
#    name: UserService
    name: user-service

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defaultZone: http://localhost:8761/eureka
